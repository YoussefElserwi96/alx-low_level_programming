#include "main.h"

/**
 * _isupper - prints and checks if its LC or UC
 * @c: to be checked by ASCII value
 * Return: 1 if c is lowercase or Returns 0 otherwise
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
