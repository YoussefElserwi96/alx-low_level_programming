#include <stdio.h>

/**
 * main -is to print out with proper grammar,
 * but the outcome is a piece of art.
 *return 0 when executed properly
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art\n");
	return (0);
}
