#include <stdio.h>

/**
 * main - printing using the command puts
 * Return: 0 if exucuted correctly.
 * Y.M
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
