#include "main.h"

/**
 * _abs - computes the value of an integer
 * @num : to be checked
 * Return: Always 0 (Success)
 */
int _abs(int num1)
{
	if (num1 >= 0)
	{
		return (num1);
	}
	return (-num1);
}
